# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Achievement {
  description: String
  difficulty: Difficulty!
  gameId: Int!
  id: Int!
  title: String!
}

union Characterunion = Hero | Villain

input CreateAchievementInput {
  description: String!
  difficulty: Difficulty!
  gameId: Int!
  title: String!
}

input CreateGameInput {
  genre: String!
  name: String!
}

"""Difficulty levels for achievements"""
enum Difficulty {
  Easy
  HARD
  MEDIUM
}

type Game {
  achievements(limit: Int, offset: Int): [Achievement!]
  genre: String!
  id: Int!
  name: String!
}

type Hero {
  name: String!
  power: String!
  weapon: String!
}

type Mutation {
  createAchievement(input: CreateAchievementInput!): Achievement!
  createGame(input1: CreateGameInput!): Game!
}

type Query {
  Achievements: [Achievement!]!
  game(id: Int!): Game!
  gameWithOffset(limit: Int = 10, offset: Int = 0): [Game!]!
  games: [Game!]!
  getCharacters: [Characterunion!]!
}

type Subscription {
  gameCreated(genre: String!): Game!
}

type Villain {
  evilPlan: String!
  name: String!
  power: String!
}