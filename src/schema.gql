# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Achievement {
  description: String
  difficulty: Difficulty!
  gameId: Int!
  id: Int!
  title: String!
}

interface Character {
  friends: [Character!]
  name: String!
  power: String!
}

input CreateAchievementInput {
  description: String!
  difficulty: Difficulty!
  gameId: Int!
  title: String!
}

input CreateGameInput {
  genre: String!
  name: String!
}

"""Difficulty levels for achievements"""
enum Difficulty {
  Easy
  HARD
  MEDIUM
}

type Game {
  achievements(limit: Int, offset: Int): [Achievement!]
  genre: String!
  id: Int!
  name: String!
}

type Hero implements Character {
  friends: [Character!]
  name: String!
  power: String!
  weapon: String!
}

type Mutation {
  createAchievement(input: CreateAchievementInput!): Achievement!
  createGame(input1: CreateGameInput!): Game!
}

type Query {
  Achievements: [Achievement!]!
  game(id: Int!): Game!
  gameWithOffset(limit: Int = 10, offset: Int = 0): [Game!]!
  games: [Game!]!
  getCharacters: [Character!]!
}

type Subscription {
  gameCreated(genre: String!): Game!
}

type Villain implements Character {
  evilPlan: String!
  friends: [Character!]
  name: String!
  power: String!
}